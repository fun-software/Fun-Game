namespace Game;

enum GamePhase: byte {
  Lobby,
  InGame,
  Post,
  Ended,
}

table PlayerRoles {
  red: uint64;
  blue: uint64;
  green: uint64;
  yellow: uint64;
}

table Game {
  id: uint64;
  phase: GamePhase;
  players: PlayerRoles;
  starttime: uint64;
  endtime: uint64;
}

root_type Game;