namespace Game;

enum GamePhase: byte {
  Lobby,
  InGame,
  Post,
  Ended,
}

table Game {
  id: string; // UUID string
  phase: GamePhase;
  players: [string];
  start_time: uint64;
  end_time: uint64;
}

root_type Game;
