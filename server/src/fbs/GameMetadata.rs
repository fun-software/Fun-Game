// automatically generated by the FlatBuffers compiler, do not modify


// @generated

use core::mem;
use core::cmp::Ordering;

extern crate flatbuffers;
use self::flatbuffers::{EndianScalar, Follow};

#[allow(unused_imports, dead_code)]
pub mod gamemetadata {

  use core::mem;
  use core::cmp::Ordering;

  extern crate flatbuffers;
  use self::flatbuffers::{EndianScalar, Follow};

#[deprecated(since = "2.0.0", note = "Use associated constants instead. This will no longer be generated in 2021.")]
pub const ENUM_MIN_GAME_PHASE: i8 = 0;
#[deprecated(since = "2.0.0", note = "Use associated constants instead. This will no longer be generated in 2021.")]
pub const ENUM_MAX_GAME_PHASE: i8 = 3;
#[deprecated(since = "2.0.0", note = "Use associated constants instead. This will no longer be generated in 2021.")]
#[allow(non_camel_case_types)]
pub const ENUM_VALUES_GAME_PHASE: [GamePhase; 4] = [
  GamePhase::Lobby,
  GamePhase::InGame,
  GamePhase::Post,
  GamePhase::Ended,
];

#[derive(Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, Default)]
#[repr(transparent)]
pub struct GamePhase(pub i8);
#[allow(non_upper_case_globals)]
impl GamePhase {
  pub const Lobby: Self = Self(0);
  pub const InGame: Self = Self(1);
  pub const Post: Self = Self(2);
  pub const Ended: Self = Self(3);

  pub const ENUM_MIN: i8 = 0;
  pub const ENUM_MAX: i8 = 3;
  pub const ENUM_VALUES: &'static [Self] = &[
    Self::Lobby,
    Self::InGame,
    Self::Post,
    Self::Ended,
  ];
  /// Returns the variant's name or "" if unknown.
  pub fn variant_name(self) -> Option<&'static str> {
    match self {
      Self::Lobby => Some("Lobby"),
      Self::InGame => Some("InGame"),
      Self::Post => Some("Post"),
      Self::Ended => Some("Ended"),
      _ => None,
    }
  }
}
impl core::fmt::Debug for GamePhase {
  fn fmt(&self, f: &mut core::fmt::Formatter) -> core::fmt::Result {
    if let Some(name) = self.variant_name() {
      f.write_str(name)
    } else {
      f.write_fmt(format_args!("<UNKNOWN {:?}>", self.0))
    }
  }
}
impl<'a> flatbuffers::Follow<'a> for GamePhase {
  type Inner = Self;
  #[inline]
  unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
    let b = flatbuffers::read_scalar_at::<i8>(buf, loc);
    Self(b)
  }
}

impl flatbuffers::Push for GamePhase {
    type Output = GamePhase;
    #[inline]
    unsafe fn push(&self, dst: &mut [u8], _written_len: usize) {
        flatbuffers::emplace_scalar::<i8>(dst, self.0);
    }
}

impl flatbuffers::EndianScalar for GamePhase {
  type Scalar = i8;
  #[inline]
  fn to_little_endian(self) -> i8 {
    self.0.to_le()
  }
  #[inline]
  #[allow(clippy::wrong_self_convention)]
  fn from_little_endian(v: i8) -> Self {
    let b = i8::from_le(v);
    Self(b)
  }
}

impl<'a> flatbuffers::Verifiable for GamePhase {
  #[inline]
  fn run_verifier(
    v: &mut flatbuffers::Verifier, pos: usize
  ) -> Result<(), flatbuffers::InvalidFlatbuffer> {
    use self::flatbuffers::Verifiable;
    i8::run_verifier(v, pos)
  }
}

impl flatbuffers::SimpleToVerifyInSlice for GamePhase {}
pub enum GameMetadataOffset {}
#[derive(Copy, Clone, PartialEq)]

pub struct GameMetadata<'a> {
  pub _tab: flatbuffers::Table<'a>,
}

impl<'a> flatbuffers::Follow<'a> for GameMetadata<'a> {
  type Inner = GameMetadata<'a>;
  #[inline]
  unsafe fn follow(buf: &'a [u8], loc: usize) -> Self::Inner {
    Self { _tab: flatbuffers::Table::new(buf, loc) }
  }
}

impl<'a> GameMetadata<'a> {
  pub const VT_ID: flatbuffers::VOffsetT = 4;
  pub const VT_PHASE: flatbuffers::VOffsetT = 6;
  pub const VT_PLAYERS: flatbuffers::VOffsetT = 8;
  pub const VT_START_TIME: flatbuffers::VOffsetT = 10;
  pub const VT_END_TIME: flatbuffers::VOffsetT = 12;

  #[inline]
  pub unsafe fn init_from_table(table: flatbuffers::Table<'a>) -> Self {
    GameMetadata { _tab: table }
  }
  #[allow(unused_mut)]
  pub fn create<'bldr: 'args, 'args: 'mut_bldr, 'mut_bldr>(
    _fbb: &'mut_bldr mut flatbuffers::FlatBufferBuilder<'bldr>,
    args: &'args GameMetadataArgs<'args>
  ) -> flatbuffers::WIPOffset<GameMetadata<'bldr>> {
    let mut builder = GameMetadataBuilder::new(_fbb);
    builder.add_end_time(args.end_time);
    builder.add_start_time(args.start_time);
    if let Some(x) = args.players { builder.add_players(x); }
    if let Some(x) = args.id { builder.add_id(x); }
    builder.add_phase(args.phase);
    builder.finish()
  }

  pub fn unpack(&self) -> GameMetadataT {
    let id = self.id().map(|x| {
      x.to_string()
    });
    let phase = self.phase();
    let players = self.players().map(|x| {
      x.iter().map(|s| s.to_string()).collect()
    });
    let start_time = self.start_time();
    let end_time = self.end_time();
    GameMetadataT {
      id,
      phase,
      players,
      start_time,
      end_time,
    }
  }

  #[inline]
  pub fn id(&self) -> Option<&'a str> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<flatbuffers::ForwardsUOffset<&str>>(GameMetadata::VT_ID, None)}
  }
  #[inline]
  pub fn phase(&self) -> GamePhase {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<GamePhase>(GameMetadata::VT_PHASE, Some(GamePhase::Lobby)).unwrap()}
  }
  #[inline]
  pub fn players(&self) -> Option<flatbuffers::Vector<'a, flatbuffers::ForwardsUOffset<&'a str>>> {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<flatbuffers::ForwardsUOffset<flatbuffers::Vector<'a, flatbuffers::ForwardsUOffset<&'a str>>>>(GameMetadata::VT_PLAYERS, None)}
  }
  #[inline]
  pub fn start_time(&self) -> u64 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u64>(GameMetadata::VT_START_TIME, Some(0)).unwrap()}
  }
  #[inline]
  pub fn end_time(&self) -> u64 {
    // Safety:
    // Created from valid Table for this object
    // which contains a valid value in this slot
    unsafe { self._tab.get::<u64>(GameMetadata::VT_END_TIME, Some(0)).unwrap()}
  }
}

impl flatbuffers::Verifiable for GameMetadata<'_> {
  #[inline]
  fn run_verifier(
    v: &mut flatbuffers::Verifier, pos: usize
  ) -> Result<(), flatbuffers::InvalidFlatbuffer> {
    use self::flatbuffers::Verifiable;
    v.visit_table(pos)?
     .visit_field::<flatbuffers::ForwardsUOffset<&str>>("id", Self::VT_ID, false)?
     .visit_field::<GamePhase>("phase", Self::VT_PHASE, false)?
     .visit_field::<flatbuffers::ForwardsUOffset<flatbuffers::Vector<'_, flatbuffers::ForwardsUOffset<&'_ str>>>>("players", Self::VT_PLAYERS, false)?
     .visit_field::<u64>("start_time", Self::VT_START_TIME, false)?
     .visit_field::<u64>("end_time", Self::VT_END_TIME, false)?
     .finish();
    Ok(())
  }
}
pub struct GameMetadataArgs<'a> {
    pub id: Option<flatbuffers::WIPOffset<&'a str>>,
    pub phase: GamePhase,
    pub players: Option<flatbuffers::WIPOffset<flatbuffers::Vector<'a, flatbuffers::ForwardsUOffset<&'a str>>>>,
    pub start_time: u64,
    pub end_time: u64,
}
impl<'a> Default for GameMetadataArgs<'a> {
  #[inline]
  fn default() -> Self {
    GameMetadataArgs {
      id: None,
      phase: GamePhase::Lobby,
      players: None,
      start_time: 0,
      end_time: 0,
    }
  }
}

pub struct GameMetadataBuilder<'a: 'b, 'b> {
  fbb_: &'b mut flatbuffers::FlatBufferBuilder<'a>,
  start_: flatbuffers::WIPOffset<flatbuffers::TableUnfinishedWIPOffset>,
}
impl<'a: 'b, 'b> GameMetadataBuilder<'a, 'b> {
  #[inline]
  pub fn add_id(&mut self, id: flatbuffers::WIPOffset<&'b  str>) {
    self.fbb_.push_slot_always::<flatbuffers::WIPOffset<_>>(GameMetadata::VT_ID, id);
  }
  #[inline]
  pub fn add_phase(&mut self, phase: GamePhase) {
    self.fbb_.push_slot::<GamePhase>(GameMetadata::VT_PHASE, phase, GamePhase::Lobby);
  }
  #[inline]
  pub fn add_players(&mut self, players: flatbuffers::WIPOffset<flatbuffers::Vector<'b , flatbuffers::ForwardsUOffset<&'b  str>>>) {
    self.fbb_.push_slot_always::<flatbuffers::WIPOffset<_>>(GameMetadata::VT_PLAYERS, players);
  }
  #[inline]
  pub fn add_start_time(&mut self, start_time: u64) {
    self.fbb_.push_slot::<u64>(GameMetadata::VT_START_TIME, start_time, 0);
  }
  #[inline]
  pub fn add_end_time(&mut self, end_time: u64) {
    self.fbb_.push_slot::<u64>(GameMetadata::VT_END_TIME, end_time, 0);
  }
  #[inline]
  pub fn new(_fbb: &'b mut flatbuffers::FlatBufferBuilder<'a>) -> GameMetadataBuilder<'a, 'b> {
    let start = _fbb.start_table();
    GameMetadataBuilder {
      fbb_: _fbb,
      start_: start,
    }
  }
  #[inline]
  pub fn finish(self) -> flatbuffers::WIPOffset<GameMetadata<'a>> {
    let o = self.fbb_.end_table(self.start_);
    flatbuffers::WIPOffset::new(o.value())
  }
}

impl core::fmt::Debug for GameMetadata<'_> {
  fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
    let mut ds = f.debug_struct("GameMetadata");
      ds.field("id", &self.id());
      ds.field("phase", &self.phase());
      ds.field("players", &self.players());
      ds.field("start_time", &self.start_time());
      ds.field("end_time", &self.end_time());
      ds.finish()
  }
}
#[non_exhaustive]
#[derive(Debug, Clone, PartialEq)]
pub struct GameMetadataT {
  pub id: Option<String>,
  pub phase: GamePhase,
  pub players: Option<Vec<String>>,
  pub start_time: u64,
  pub end_time: u64,
}
impl Default for GameMetadataT {
  fn default() -> Self {
    Self {
      id: None,
      phase: GamePhase::Lobby,
      players: None,
      start_time: 0,
      end_time: 0,
    }
  }
}
impl GameMetadataT {
  pub fn pack<'b>(
    &self,
    _fbb: &mut flatbuffers::FlatBufferBuilder<'b>
  ) -> flatbuffers::WIPOffset<GameMetadata<'b>> {
    let id = self.id.as_ref().map(|x|{
      _fbb.create_string(x)
    });
    let phase = self.phase;
    let players = self.players.as_ref().map(|x|{
      let w: Vec<_> = x.iter().map(|s| _fbb.create_string(s)).collect();_fbb.create_vector(&w)
    });
    let start_time = self.start_time;
    let end_time = self.end_time;
    GameMetadata::create(_fbb, &GameMetadataArgs{
      id,
      phase,
      players,
      start_time,
      end_time,
    })
  }
}
#[inline]
/// Verifies that a buffer of bytes contains a `GameMetadata`
/// and returns it.
/// Note that verification is still experimental and may not
/// catch every error, or be maximally performant. For the
/// previous, unchecked, behavior use
/// `root_as_game_metadata_unchecked`.
pub fn root_as_game_metadata(buf: &[u8]) -> Result<GameMetadata, flatbuffers::InvalidFlatbuffer> {
  flatbuffers::root::<GameMetadata>(buf)
}
#[inline]
/// Verifies that a buffer of bytes contains a size prefixed
/// `GameMetadata` and returns it.
/// Note that verification is still experimental and may not
/// catch every error, or be maximally performant. For the
/// previous, unchecked, behavior use
/// `size_prefixed_root_as_game_metadata_unchecked`.
pub fn size_prefixed_root_as_game_metadata(buf: &[u8]) -> Result<GameMetadata, flatbuffers::InvalidFlatbuffer> {
  flatbuffers::size_prefixed_root::<GameMetadata>(buf)
}
#[inline]
/// Verifies, with the given options, that a buffer of bytes
/// contains a `GameMetadata` and returns it.
/// Note that verification is still experimental and may not
/// catch every error, or be maximally performant. For the
/// previous, unchecked, behavior use
/// `root_as_game_metadata_unchecked`.
pub fn root_as_game_metadata_with_opts<'b, 'o>(
  opts: &'o flatbuffers::VerifierOptions,
  buf: &'b [u8],
) -> Result<GameMetadata<'b>, flatbuffers::InvalidFlatbuffer> {
  flatbuffers::root_with_opts::<GameMetadata<'b>>(opts, buf)
}
#[inline]
/// Verifies, with the given verifier options, that a buffer of
/// bytes contains a size prefixed `GameMetadata` and returns
/// it. Note that verification is still experimental and may not
/// catch every error, or be maximally performant. For the
/// previous, unchecked, behavior use
/// `root_as_game_metadata_unchecked`.
pub fn size_prefixed_root_as_game_metadata_with_opts<'b, 'o>(
  opts: &'o flatbuffers::VerifierOptions,
  buf: &'b [u8],
) -> Result<GameMetadata<'b>, flatbuffers::InvalidFlatbuffer> {
  flatbuffers::size_prefixed_root_with_opts::<GameMetadata<'b>>(opts, buf)
}
#[inline]
/// Assumes, without verification, that a buffer of bytes contains a GameMetadata and returns it.
/// # Safety
/// Callers must trust the given bytes do indeed contain a valid `GameMetadata`.
pub unsafe fn root_as_game_metadata_unchecked(buf: &[u8]) -> GameMetadata {
  flatbuffers::root_unchecked::<GameMetadata>(buf)
}
#[inline]
/// Assumes, without verification, that a buffer of bytes contains a size prefixed GameMetadata and returns it.
/// # Safety
/// Callers must trust the given bytes do indeed contain a valid size prefixed `GameMetadata`.
pub unsafe fn size_prefixed_root_as_game_metadata_unchecked(buf: &[u8]) -> GameMetadata {
  flatbuffers::size_prefixed_root_unchecked::<GameMetadata>(buf)
}
#[inline]
pub fn finish_game_metadata_buffer<'a, 'b>(
    fbb: &'b mut flatbuffers::FlatBufferBuilder<'a>,
    root: flatbuffers::WIPOffset<GameMetadata<'a>>) {
  fbb.finish(root, None);
}

#[inline]
pub fn finish_size_prefixed_game_metadata_buffer<'a, 'b>(fbb: &'b mut flatbuffers::FlatBufferBuilder<'a>, root: flatbuffers::WIPOffset<GameMetadata<'a>>) {
  fbb.finish_size_prefixed(root, None);
}
}  // pub mod Gamemetadata

