name: Fun-Software CI/CD

on:
  push:
    branches:
      - main
      - ci

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Server tests
      run: |
        cd server
        echo "I do quite literally nothing"
    - name: Client tests
      run: |
        cd client
        npm run test
